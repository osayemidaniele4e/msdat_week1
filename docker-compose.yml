version: "3.8"

services:
  msdat-frontend:
    image: registry.gitlab.com/e4e-webdev/msdat:develop
    container_name: msdat_frontend
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: always

